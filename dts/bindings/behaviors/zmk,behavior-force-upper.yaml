description: Force Uppercase behavior (always sends uppercase letter regardless of Caps Lock state)

compatible: "zmk,behavior-force-upper"

include: one_param.yaml

properties:
  label:
    type: string
    required: false
